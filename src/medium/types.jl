# consider implementing a matter type, which has the following features
# - consists a matter model (<:AbstractMatterModel)
# - can be constructed from elements
# - can be constructed from composits/mixtures
# - allows initialization from different kwargs (rs, rho, ... )
# - implements the matter model interface (response function, dsf,...)
